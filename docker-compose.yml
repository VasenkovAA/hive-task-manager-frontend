version: '3.8'

services:
  hive-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hive-frontend
    ports:
      - "80:80"
    restart: unless-stopped
    networks:
      - hive-network

networks:
  hive-network:
    driver: bridge